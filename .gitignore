.DS_Store
*.tmp
*.bak
*.swp

# 含真实订阅链接，禁止提交
clash/src/secrets.yaml

# 跨机器交接笔记（可能包含 secrets.yaml 内容），禁止提交
HANDOFF.md

# 构建输出 (含真实订阅链接，仅本地使用)
clash/openclash/dist/
clash/stash/dist/

# shadowrocket/dist/ 纳入 git 管理 (不含订阅信息)

# Claude Code 内部目录
.claude/
